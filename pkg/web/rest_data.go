package web

const (
	kHome = "/"

	kTest = "/test/:contestID/:problemIndex"

	kGlobalStatus = "/status"

	kTicketStatus = "/status/:ticket"

	kMailingList = "/mailing-list"

	kSimulateStressTest = "/internal/simulate-concurrent-users"
)
